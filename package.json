{
  "name": "catland",
  "version": "1.0.0",
  "description": "il faut une ligne start qui sera -> start: node index.js - mais ici on utilise nodemon, donc ce sera -> nodemon index.js",
  "main": "index.js",
  "scripts": {
    "start": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "nodemon": "^3.0.2"
  }
}
